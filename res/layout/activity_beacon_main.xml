<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="${relativePackage}.${activityClass}" >
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dip"
        android:orientation="horizontal">
	    <ImageButton
	        android:id="@+id/btn_start_guard_mode"
	        android:layout_width="wrap_content"
	        android:layout_height="match_parent"
	        android:src="@android:drawable/ic_lock_lock"
	        android:contentDescription="Start Guard Mode"
            android:paddingLeft="25dip"
            android:paddingRight="25dip" />
	    
	    <ImageButton
	        android:id="@+id/btn_mode"
            android:layout_width="wrap_content"
	        android:layout_height="match_parent"
	        android:src="@android:drawable/ic_lock_power_off"
	        android:contentDescription="Set Mode"
            android:paddingLeft="25dip"
            android:paddingRight="25dip" />

        <ImageButton
            android:id="@+id/btn_configure"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@android:drawable/ic_menu_preferences"
            android:contentDescription="Configure"
            android:paddingLeft="25dip"
            android:paddingRight="25dip" />
    </LinearLayout>
    
    <TextView
            android:id="@+id/txv_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="State: UNKNOWN" />
    
    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ListView>

</LinearLayout>
